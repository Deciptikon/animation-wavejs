const animationExporter = {
  exportGIF() {
    if (animationPlayer.frames.length === 0) {
      alert("Нет кадров для экспорта");
      return;
    }

    alert("Экспорт GIF будет реализован здесь");
    // Здесь будет код для создания и скачивания GIF
  },
};
